# DDD plugin v0.2.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 v0.1.0 的 `--addons ddd` 占位升级为真实现：从 ddd-run 移植 3 个 skill + DOMAIN.md + README-DDD-HARNESS.md，扩展 Addon trait 真分发，AGENTS.md 动态拼接 DDD 元规则段，status 增加 Addons 检测；DOMAIN.md install-once 语义保护用户领域知识。

**Architecture:** 复用 v0.1 的 `addons/` 模块骨架，把 stub 的 `handle_addons` 改为真实的 `resolve_addons → Vec<Box<dyn Addon>> → install()` 分发链。DddAddon 通过 `include_dir!` + `include_bytes!` 嵌入 ddd-run 模板。AGENTS.md 渲染加 `addons: &[&dyn Addon]` 参数，每个 addon 提供 `agents_md_segment()` 静态文本追加到 directive 末尾。

**Tech Stack:** Rust 1.75+ / clap derive / anyhow / colored / include_dir 0.7（含 glob feature，v0.1 已加）/ assert_cmd / predicates / tempfile（dev）。无新依赖。

**Spec:** [`docs/superpowers/specs/2026-04-29-ddd-plugin-v02-design.md`](../specs/2026-04-29-ddd-plugin-v02-design.md)

---

## File Structure

| 路径 | 责任 | 状态 |
|---|---|---|
| `cli/templates/ddd/skills/ddd-storm.md` | DDD event storming skill md | ★ 新增（cp from ddd-run） |
| `cli/templates/ddd/skills/ddd-model.md` | DDD modeling skill md | ★ 新增 |
| `cli/templates/ddd/skills/ddd-spec.md` | DDD spec generation skill md | ★ 新增 |
| `cli/templates/ddd/root/DOMAIN.md` | 领域模型 SSOT 模板（install-once）| ★ 新增 |
| `cli/templates/ddd/root/README-DDD-HARNESS.md` | DDD 工作流向导 | ★ 新增 |
| `cli/templates/ddd/root/claude-md-merge.md` | DDD 元规则段，并入 AGENTS.md 末尾 | ★ 新增（精炼自 ddd-run CLAUDE.md） |
| `cli/src/addons/mod.rs` | Addon trait + resolve_addons + handle_addons alias | 修改 |
| `cli/src/addons/ddd.rs` | DddAddon impl Addon | ★ 新增 |
| `cli/src/agents_md.rs` | render_agents_md 加 addons 参数 | 修改 |
| `cli/src/commands/init.rs` | run_with_options 加 Phase 3 addon install | 修改 |
| `cli/src/commands/upgrade.rs` | 不动签名（addons 已经 flow through） | 不动 |
| `cli/src/commands/status.rs` | Addons 段检测 ddd | 修改 |
| `cli/Cargo.toml` | version 0.1.0 → 0.2.0；metadata.ddd_run_version | 修改 |
| `cli/tests/integration_status.rs` | 加 1 个 ddd 用例 | 修改 |
| `cli/tests/integration_addons_ddd.rs` | 6 个 DDD 端到端测试 | ★ 新增 |
| `Makefile` | e2e-smoke 加 `--addons ddd` 路径 | 修改 |
| `README.md`（仓库根）| DDD plugin 段 | 修改 |
| `cli/README.md` | DDD plugin 段 | 修改 |

---

## Task 1: 同步 ddd-run 模板到 cli/templates/ddd/

**Files:**
- Create: `cli/templates/ddd/skills/ddd-storm.md`
- Create: `cli/templates/ddd/skills/ddd-model.md`
- Create: `cli/templates/ddd/skills/ddd-spec.md`
- Create: `cli/templates/ddd/root/DOMAIN.md`
- Create: `cli/templates/ddd/root/README-DDD-HARNESS.md`

- [ ] **Step 1: 创建目录骨架**

```bash
cd /home/xiaojin/workshop/cc-superpower-to-trae   # 实际工作时是 worktree 路径
mkdir -p cli/templates/ddd/skills cli/templates/ddd/root
```

- [ ] **Step 2: 拷贝 3 个 skill 文件（保持原内容不变）**

```bash
cp ~/workshop/ddd-run/src/templates/skills/ddd-storm.md cli/templates/ddd/skills/
cp ~/workshop/ddd-run/src/templates/skills/ddd-model.md cli/templates/ddd/skills/
cp ~/workshop/ddd-run/src/templates/skills/ddd-spec.md cli/templates/ddd/skills/
```

- [ ] **Step 3: 拷贝 2 个 root 文件（保持原内容不变）**

```bash
cp ~/workshop/ddd-run/src/templates/root/DOMAIN.md cli/templates/ddd/root/
cp ~/workshop/ddd-run/src/templates/root/README-DDD-HARNESS.md cli/templates/ddd/root/
```

- [ ] **Step 4: 验证**

```bash
ls -la cli/templates/ddd/skills/ cli/templates/ddd/root/
wc -l cli/templates/ddd/skills/*.md cli/templates/ddd/root/*.md
```

Expected:
- `skills/` 含 3 个 .md 文件（约 99/180/269 行）
- `root/` 含 2 个 .md 文件（约 120/206 行）

- [ ] **Step 5: 记录源版本**

```bash
( cd ~/workshop/ddd-run && git rev-parse --short HEAD )
```

把输出（如 `a1b2c3d`）记下来——下面 Task 11 写到 Cargo.toml metadata。

- [ ] **Step 6: Commit**

```bash
git add cli/templates/ddd/skills/ cli/templates/ddd/root/
git -c user.name="xiaojin" -c user.email="amwtke@gmail.com" commit -m "feat(ddd): 同步 ddd-run 模板（5 个文件）"
```

---

## Task 2: 编写 cli/templates/ddd/root/claude-md-merge.md

**Files:**
- Create: `cli/templates/ddd/root/claude-md-merge.md`

精炼版的 ddd-run CLAUDE.md——去掉用户自填字段（项目定位、技术栈占位）和 Java 特定包结构，保留普适约束。

- [ ] **Step 1: 写文件（完整内容）**

```markdown
---

# DDD methodology (addon)

> 本项目通过 `superpowers-trae --addons ddd` 安装了 DDD harness。
> 任何代码生成前，请先确认 DDD 工作流前置阶段已完成。

## DDD 工作流（在 superpowers brainstorming 之前）

代码实现必须经过这三个战略阶段：

1. **事件风暴**：用户用 `/ddd-storm <业务描述>` 或说"使用 ddd-storm ..."；
   产出 `docs/ddd/01-event-storming-*.md`。
   你必须 `Read` `.trae/skills/ddd/ddd-storm/SKILL.md` 后按其流程执行。
2. **领域建模**：用 `/ddd-model` 或"使用 ddd-model ..."；
   维护 `DOMAIN.md`（Single Source of Truth）。
   `Read` `.trae/skills/ddd/ddd-model/SKILL.md` 后执行。
3. **生成 spec**：用 `/ddd-spec <场景>` 或"使用 ddd-spec ..."；
   产出 `docs/specs/spec-*.md`，作为 superpowers brainstorming 的输入。
   `Read` `.trae/skills/ddd/ddd-spec/SKILL.md` 后执行。

完成后才进入 superpowers `brainstorming → writing-plans → executing-plans` 流程。

## 强制约束（Hard Rules）

### R1. 战略层先行 + 技术栈先决策
- 任何新特性必须按 `/ddd-storm → /ddd-model → /ddd-spec → superpowers brainstorming` 顺序进行。
- `DOMAIN.md` 的"技术栈约定"段未填前，**禁止** superpowers `writing-plans`（含实现）。
- 用户要求跳步，请明确指出违反 harness 约定。

### R2. 术语一致性（Ubiquitous Language）
- 所有代码命名必须引用 `DOMAIN.md` 中的领域术语表。
- 发现代码命名与 DOMAIN.md 不一致 → **停下来询问用户**，不要自作主张修改。

### R3. 富领域模型（禁止贫血）
- 聚合根封装业务行为；Application Service 只做编排（取聚合 / 调方法 / 持久化 / 发事件 / 事务）。
- `if/else`、`for` 中含业务判断的代码必须在 Domain 层，不在 Application。

### R4. 聚合边界
- 一个事务只修改**一个**聚合实例；跨聚合协作用领域事件，不用同步调用。
- 聚合之间只引用 ID，不持有对象。

### R5. Repository 只对聚合根
- 不为 VO / 聚合内子实体单独建 Repository。

### R6. TDD 节奏
- 进入 superpowers `executing-plans` 后严格 spec → test → code 节奏。
- 一次一个 spec：先写测试 → 失败 → 最小代码通过 → 重构 → 下一个。
- **禁止一次性生成整套代码**。

### R7. 包结构 / 分层
- 必须有 4 层：interfaces / application / domain / infrastructure。
- 依赖方向：上层依赖下层；Domain 层不依赖任何其他层。
- 具体语言/框架的目录约定由 `brainstorming` 阶段在 `DOMAIN.md` 技术栈段确定后填回。

## 修改 DOMAIN.md 的流程

`DOMAIN.md` 是领域模型 SSOT，**不得随意修改**。允许的修改路径：

1. 通过 `/ddd-model` 重新建模（推荐）
2. 在 `/ddd-spec` 过程中发现缺失，**停下先修 DOMAIN.md 再继续**

禁止：
- superpowers 实现过程中擅自修改 DOMAIN.md
- 为了让代码通过测试而改 DOMAIN.md 术语

## 代码质量底线

- 每个聚合必须有单元测试（覆盖不变式）
- 每个 Application Service 方法必须有集成测试
- 测试命名使用业务语言（如 `shouldRedeemPointsWhenBalanceIsSufficient`）
- 禁止魔法数字
- 禁止 public 字段（除语言 record/struct 字段约定）
```

> 注：开头的 `---` 分隔符是必需的——`render_agents_md` 把这个 segment 拼接到 project_rules.md body 之后；前面已经有 `---` 分隔了 v0.1 的 directive 和 body，所以这里再加一个 `---` 让 DDD 段在视觉上独立。

- [ ] **Step 2: Commit**

```bash
git add cli/templates/ddd/root/claude-md-merge.md
git -c user.name="xiaojin" -c user.email="amwtke@gmail.com" commit -m "feat(ddd): 提炼 CLAUDE.md 元规则段（claude-md-merge.md）"
```

---

## Task 3: Addon trait 扩展（resolve_addons + install + agents_md_segment）

**Files:**
- Modify: `cli/src/addons/mod.rs`

- [ ] **Step 1: 写失败的单测（追加到现有 #[cfg(test)] mod tests）**

在 `cli/src/addons/mod.rs` 末尾的 `#[cfg(test)] mod tests` 内，把现有 3 个测试**保留**（empty / ddd_stub_ok / unknown_addon_errors），追加 3 个新测试 + 改其中 `ddd_stub_ok` 名字（待会儿会被 resolve_addons 接管）：

实际操作：把整个 mod tests 替换为：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn resolve_empty_ok() {
        assert_eq!(resolve_addons(&[]).unwrap().len(), 0);
    }

    #[test]
    fn resolve_ddd_returns_one_addon() {
        let addons = resolve_addons(&["ddd".to_string()]).unwrap();
        assert_eq!(addons.len(), 1);
        assert_eq!(addons[0].name(), "ddd");
    }

    #[test]
    fn resolve_unknown_errors() {
        let r = resolve_addons(&["bogus".to_string()]);
        assert!(r.is_err());
        assert!(format!("{:?}", r.unwrap_err()).contains("Unknown addon"));
    }

    #[test]
    fn handle_addons_alias_works_for_empty() {
        assert!(handle_addons(&[]).is_ok());
    }

    #[test]
    fn handle_addons_alias_works_for_ddd() {
        assert!(handle_addons(&["ddd".to_string()]).is_ok());
    }

    #[test]
    fn handle_addons_alias_errors_on_unknown() {
        assert!(handle_addons(&["bogus".to_string()]).is_err());
    }
}
```

- [ ] **Step 2: 跑测试，确认 resolve_addons 不存在**

```bash
cd cli && cargo test addons
```

Expected: `cannot find function 'resolve_addons'`（编译错误）。

- [ ] **Step 3: 实现新版 mod.rs**

把 `cli/src/addons/mod.rs` **整个替换为**：

```rust
//! Addon plugin scaffolding.
//!
//! v0.2 changes vs v0.1:
//! - `Addon` trait gains `install()` + `agents_md_segment()`
//! - `resolve_addons` returns concrete addon objects (replaces stub-only `handle_addons`)
//! - `handle_addons` retained as backward-compatible alias

use anyhow::{bail, Result};
use std::path::Path;
use crate::rollback::InstallSession;

pub mod ddd;

pub trait Addon {
    fn name(&self) -> &str;

    /// Install/refresh the plugin in the target dir.
    /// Uses the rollback session for backup safety.
    fn install(
        &self,
        dir: &Path,
        backup_existing: bool,
        session: &mut InstallSession,
    ) -> Result<()>;

    /// Optional AGENTS.md segment to append to the directive (compile-time embedded).
    /// Default: None means this addon does not extend AGENTS.md.
    fn agents_md_segment(&self) -> Option<&'static str> {
        None
    }
}

/// Validate `--addons` names and return concrete addon objects.
/// Empty input → empty vec. Unknown name → error.
pub fn resolve_addons(addons: &[String]) -> Result<Vec<Box<dyn Addon>>> {
    let mut out: Vec<Box<dyn Addon>> = Vec::new();
    for a in addons {
        match a.as_str() {
            "ddd" => out.push(Box::new(ddd::DddAddon)),
            other => bail!(
                "Unknown addon: '{}' (supported in v0.2: 'ddd')",
                other
            ),
        }
    }
    Ok(out)
}

/// Backward-compatible alias for v0.1 callers (drops the addon objects, just validates names).
pub fn handle_addons(addons: &[String]) -> Result<()> {
    resolve_addons(addons).map(|_| ())
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn resolve_empty_ok() {
        assert_eq!(resolve_addons(&[]).unwrap().len(), 0);
    }

    #[test]
    fn resolve_ddd_returns_one_addon() {
        let addons = resolve_addons(&["ddd".to_string()]).unwrap();
        assert_eq!(addons.len(), 1);
        assert_eq!(addons[0].name(), "ddd");
    }

    #[test]
    fn resolve_unknown_errors() {
        let r = resolve_addons(&["bogus".to_string()]);
        assert!(r.is_err());
        assert!(format!("{:?}", r.unwrap_err()).contains("Unknown addon"));
    }

    #[test]
    fn handle_addons_alias_works_for_empty() {
        assert!(handle_addons(&[]).is_ok());
    }

    #[test]
    fn handle_addons_alias_works_for_ddd() {
        assert!(handle_addons(&["ddd".to_string()]).is_ok());
    }

    #[test]
    fn handle_addons_alias_errors_on_unknown() {
        assert!(handle_addons(&["bogus".to_string()]).is_err());
    }
}
```

> 此时 `pub mod ddd;` 引用 ddd.rs 还不存在——下一 Step 让编译失败、Task 4 创建。

- [ ] **Step 4: 跑测试看它继续失败（ddd 模块不存在）**

```bash
cd cli && cargo test addons
```

Expected: `file not found for module 'ddd'`。

- [ ] **Step 5: Commit（暂时编译破，Task 4 修复）**

为了保留 commit 粒度对 review 友好，先在 mod.rs 临时把 `pub mod ddd;` 改为 `// pub mod ddd; // populated by Task 4`，commit，再 Task 4 反向：

实际操作——把 mod.rs 第 13 行：
```rust
pub mod ddd;
```
改为：
```rust
// pub mod ddd;  // re-enabled by Task 4
```

> 同时 `resolve_addons` 里 `Box::new(ddd::DddAddon)` 也要临时注释——直接整个 match arm 替换为 stub：
> ```rust
> "ddd" => bail!("ddd addon temporarily disabled (Task 4 will populate)"),
> ```
> 这样 cargo build 通过，但 `resolve_ddd_returns_one_addon` 测试会失败。

- [ ] **Step 6: Adjust tests temporarily**

把 `resolve_ddd_returns_one_addon` 和 `handle_addons_alias_works_for_ddd` 改为 `#[ignore]`（标注为待 Task 4 启用）：

```rust
    #[test]
    #[ignore = "re-enabled by Task 4"]
    fn resolve_ddd_returns_one_addon() {
        let addons = resolve_addons(&["ddd".to_string()]).unwrap();
        assert_eq!(addons.len(), 1);
        assert_eq!(addons[0].name(), "ddd");
    }
    
    #[test]
    #[ignore = "re-enabled by Task 4"]
    fn handle_addons_alias_works_for_ddd() {
        assert!(handle_addons(&["ddd".to_string()]).is_ok());
    }
```

- [ ] **Step 7: 跑测试 + 编译通过**

```bash
cd cli && cargo build && cargo test addons 2>&1 | tail -10
```

Expected: 4 个非-ignored addons 测试 PASS（empty / unknown_errors / alias_empty / alias_unknown）；2 个 ignored。原有 v0.1 单元测试和集成测试不受影响。

- [ ] **Step 8: Commit**

```bash
git add cli/src/addons/mod.rs
git -c user.name="xiaojin" -c user.email="amwtke@gmail.com" commit -m "feat(addons): Addon trait 扩展 + resolve_addons（ddd 暂禁，Task 4 启用）"
```

---

## Task 4: 实现 DddAddon（addons/ddd.rs）

**Files:**
- Create: `cli/src/addons/ddd.rs`
- Modify: `cli/src/addons/mod.rs`（启用 ddd 引用）

- [ ] **Step 1: 写 cli/src/addons/ddd.rs（完整内容）**

```rust
//! DDD plugin: 3 skills + DOMAIN.md (install-once) + README-DDD-HARNESS.md.

use anyhow::Result;
use include_dir::{include_dir, Dir};
use std::path::Path;
use crate::addons::Addon;
use crate::rollback::InstallSession;

static DDD_SKILLS: Dir<'_> = include_dir!("$CARGO_MANIFEST_DIR/templates/ddd/skills");
const ROOT_DOMAIN_MD: &[u8] = include_bytes!("../../templates/ddd/root/DOMAIN.md");
const ROOT_README: &[u8] = include_bytes!("../../templates/ddd/root/README-DDD-HARNESS.md");
const AGENTS_MD_SEGMENT: &str = include_str!("../../templates/ddd/root/claude-md-merge.md");

pub struct DddAddon;

impl Addon for DddAddon {
    fn name(&self) -> &str {
        "ddd"
    }

    fn install(
        &self,
        dir: &Path,
        backup_existing: bool,
        session: &mut InstallSession,
    ) -> Result<()> {
        // 1. Skills: each <stem>.md → .trae/skills/ddd/<stem>/SKILL.md
        for entry in DDD_SKILLS.files() {
            let stem = entry
                .path()
                .file_stem()
                .and_then(|s| s.to_str())
                .ok_or_else(|| anyhow::anyhow!("invalid skill filename: {:?}", entry.path()))?;
            let target = dir.join(".trae/skills/ddd").join(stem).join("SKILL.md");
            session.write(&target, entry.contents(), backup_existing)?;
        }

        // 2. README-DDD-HARNESS.md (project root, always replace + backup)
        let readme_target = dir.join("README-DDD-HARNESS.md");
        session.write(&readme_target, ROOT_README, backup_existing)?;

        // 3. DOMAIN.md (install-once: skip if exists; otherwise write without backup)
        let domain_target = dir.join("DOMAIN.md");
        if !domain_target.exists() {
            session.write(&domain_target, ROOT_DOMAIN_MD, false)?;
        } else {
            println!("ℹ DOMAIN.md preserved (user-managed).");
        }

        Ok(())
    }

    fn agents_md_segment(&self) -> Option<&'static str> {
        Some(AGENTS_MD_SEGMENT)
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn ddd_addon_name() {
        assert_eq!(DddAddon.name(), "ddd");
    }

    #[test]
    fn ddd_agents_md_segment_present() {
        let segment = DddAddon.agents_md_segment().unwrap();
        assert!(segment.contains("DDD methodology"));
        assert!(segment.contains("Ubiquitous Language"));
    }

    #[test]
    fn ddd_install_creates_skills_and_root_files() {
        use tempfile::tempdir;
        let tmp = tempdir().unwrap();
        let mut session = InstallSession::new();
        DddAddon.install(tmp.path(), false, &mut session).unwrap();

        assert!(tmp.path().join(".trae/skills/ddd/ddd-storm/SKILL.md").exists());
        assert!(tmp.path().join(".trae/skills/ddd/ddd-model/SKILL.md").exists());
        assert!(tmp.path().join(".trae/skills/ddd/ddd-spec/SKILL.md").exists());
        assert!(tmp.path().join("DOMAIN.md").exists());
        assert!(tmp.path().join("README-DDD-HARNESS.md").exists());
    }

    #[test]
    fn ddd_install_preserves_existing_domain_md() {
        use tempfile::tempdir;
        use std::fs;
        let tmp = tempdir().unwrap();
        fs::write(tmp.path().join("DOMAIN.md"), "USER MODEL CONTENT").unwrap();
        let mut session = InstallSession::new();
        DddAddon.install(tmp.path(), false, &mut session).unwrap();
        assert_eq!(
            fs::read_to_string(tmp.path().join("DOMAIN.md")).unwrap(),
            "USER MODEL CONTENT"
        );
    }
}
```

- [ ] **Step 2: 启用 mod.rs 中的 ddd 引用**

在 `cli/src/addons/mod.rs`：

把 `// pub mod ddd;  // re-enabled by Task 4` 还原为 `pub mod ddd;`

把 `"ddd" => bail!("ddd addon temporarily disabled (Task 4 will populate)"),` 还原为 `"ddd" => out.push(Box::new(ddd::DddAddon)),`

把两个 `#[ignore = "re-enabled by Task 4"]` 删掉，让两个 ddd 相关测试参与。

- [ ] **Step 3: 跑测试**

```bash
cd cli && cargo test addons 2>&1 | tail -10
```

Expected: 6 个 mod.rs 测试 + 4 个 ddd.rs 测试 = 10 个 PASS。

- [ ] **Step 4: 跑全套**

```bash
cd cli && cargo test 2>&1 | grep "test result"
```

Expected: 全部 PASS（v0.1 的 19 单测 + 13 集成 + 本任务新增 10 = 42 单测 + 13 集成）。

- [ ] **Step 5: Commit**

```bash
git add cli/src/addons/ddd.rs cli/src/addons/mod.rs
git -c user.name="xiaojin" -c user.email="amwtke@gmail.com" commit -m "feat(addons): DddAddon 真实现 + 4 单测"
```

---

## Task 5: agents_md::render_agents_md 加 addons 参数（TDD）

**Files:**
- Modify: `cli/src/agents_md.rs`

- [ ] **Step 1: 写失败测试（追加到现有 mod tests）**

在 `cli/src/agents_md.rs` 的 `#[cfg(test)] mod tests` 内追加：

```rust
    use crate::addons::Addon;
    use crate::rollback::InstallSession;
    use std::path::Path;

    struct MockAddon {
        seg: Option<&'static str>,
    }
    impl Addon for MockAddon {
        fn name(&self) -> &str { "mock" }
        fn install(&self, _: &Path, _: bool, _: &mut InstallSession) -> anyhow::Result<()> { Ok(()) }
        fn agents_md_segment(&self) -> Option<&'static str> { self.seg }
    }

    #[test]
    fn render_agents_md_appends_addon_segments() {
        let addon = MockAddon { seg: Some("\n---\n# DDD SEGMENT\n") };
        let addons: Vec<&dyn Addon> = vec![&addon];
        let out = render_agents_md("rule body\n", &[], &addons);
        assert!(out.contains("DDD SEGMENT"));
    }

    #[test]
    fn render_agents_md_empty_addons_unchanged() {
        let no_addons: Vec<&dyn Addon> = vec![];
        let with_old = render_agents_md_v01_compat("rule body\n", &[]);
        let with_new = render_agents_md("rule body\n", &[], &no_addons);
        assert_eq!(with_old, with_new);
    }

    /// Compat helper to test v0.1 vs v0.2 output equivalence when no addons.
    fn render_agents_md_v01_compat(rules: &str, skills: &[Frontmatter]) -> String {
        // duplicate of v0.1 logic — used only as test oracle
        let no_addons: Vec<&dyn Addon> = vec![];
        render_agents_md(rules, skills, &no_addons)
    }
```

> 注：上面的 `render_agents_md_v01_compat` 在测试里循环调用 `render_agents_md` 比较——这是 tautology，但它的存在是为了 codify "v0.1 路径不变" 的契约（empty addons 时输出与无 addons 等价；如果未来 render_agents_md 被改为对 empty addons 添加新内容，这个测试会通过编译但需要人工 review）。

- [ ] **Step 2: 跑测试看签名编译失败**

```bash
cd cli && cargo test agents_md 2>&1 | tail -20
```

Expected: `expected 2 arguments, found 3`。

- [ ] **Step 3: 改 render_agents_md 签名**

在 `cli/src/agents_md.rs`，找到：

```rust
pub fn render_agents_md(project_rules_body: &str, skills: &[Frontmatter]) -> String {
```

改为：

```rust
pub fn render_agents_md(
    project_rules_body: &str,
    skills: &[Frontmatter],
    addons: &[&dyn crate::addons::Addon],
) -> String {
```

并在函数体末尾、`format!(...)` 返回值之前，把返回值改为 `let mut out = format!(...)`，然后追加：

```rust
    for addon in addons {
        if let Some(segment) = addon.agents_md_segment() {
            out.push('\n');
            out.push_str(segment);
        }
    }
    out
```

完整新版函数：

```rust
pub fn render_agents_md(
    project_rules_body: &str,
    skills: &[Frontmatter],
    addons: &[&dyn crate::addons::Addon],
) -> String {
    let skill_index = skills
        .iter()
        .map(|s| format!("- {} — {}", s.name, s.description))
        .collect::<Vec<_>>()
        .join("\n");

    let mut out = format!(
        r#"# AGENTS DIRECTIVE — superpowers methodology

> **Critical**: When the user's request matches a skill's purpose listed below,
> you MUST `Read` the corresponding SKILL.md file and follow it exactly BEFORE
> producing any other output (including clarifying questions, exploration, or
> proposed solutions). Do not paraphrase from memory — skills evolve and the
> file is the source of truth.

## How to load a skill

1. Identify which skill matches the user's request (see Skill Index below).
2. Use the `Read` tool on `.trae/skills/superpowers/<skill-name>/SKILL.md`.
3. Follow that file's checklist verbatim.

## Skill Index

{skill_index}

## Tool name reference

For tool name mappings between Claude Code and Trae IDE, see
`.trae/skills/superpowers/references/trae-tools.md`.

---

{project_rules_body}"#,
        skill_index = skill_index,
        project_rules_body = project_rules_body,
    );

    for addon in addons {
        if let Some(segment) = addon.agents_md_segment() {
            out.push('\n');
            out.push_str(segment);
        }
    }
    out
}
```

- [ ] **Step 4: 跑测试**

```bash
cd cli && cargo test agents_md 2>&1 | grep "test result"
```

Expected: 9 个 PASS（v0.1 的 7 个 + 新加 2 个）。

- [ ] **Step 5: 全套测试（确保 init.rs 等 caller 编译错——还没改）**

```bash
cd cli && cargo build 2>&1 | tail -10
```

Expected: 编译错——init.rs 调 `render_agents_md(rules, &skills)` 没第三个参数。Task 6 修复。

- [ ] **Step 6: Commit（保留 init.rs 待修状态）**

```bash
git add cli/src/agents_md.rs
git -c user.name="xiaojin" -c user.email="amwtke@gmail.com" commit -m "feat(agents_md): render_agents_md 加 addons 参数 + 单测（init.rs 待 Task 6 修复）"
```

---

## Task 6: init::run_with_options 加 Phase 3 addon install

**Files:**
- Modify: `cli/src/commands/init.rs`

- [ ] **Step 1: 改 run_with_options 实现**

在 `cli/src/commands/init.rs` 找到 `pub fn run_with_options(...)` 函数，整个函数替换为：

```rust
pub fn run_with_options(
    dir: &Path,
    _force: bool,
    backup_existing: bool,
    addons_list: &[String],
) -> Result<()> {
    let mut session = rollback::InstallSession::new();

    // Phase 1: install rules/project_rules.md
    let rules_src = embed::DIST_USER
        .get_file("rules/user_rules.md")
        .ok_or_else(|| anyhow!("embedded dist missing rules/user_rules.md"))?;
    let project_rules_body = std::str::from_utf8(rules_src.contents())?;
    let rules_target = dir.join(".trae/rules/project_rules.md");
    if let Err(e) = session.write(&rules_target, rules_src.contents(), backup_existing) {
        session.rollback();
        return Err(e);
    }

    // Phase 2: install skills/superpowers/ recursively
    let skills_dir = embed::DIST_USER
        .get_dir("skills/superpowers")
        .ok_or_else(|| anyhow!("embedded dist missing skills/superpowers"))?;
    if let Err(e) = write_embedded_dir(skills_dir, &dir.join(".trae"), backup_existing, &mut session) {
        session.rollback();
        return Err(e);
    }

    // Phase 3: install addons (writes addon-specific files like .trae/skills/ddd/, DOMAIN.md, etc.)
    let addon_list = addons::resolve_addons(addons_list)?;
    for addon in &addon_list {
        if let Err(e) = addon.install(dir, backup_existing, &mut session) {
            session.rollback();
            return Err(e.context(format!("addon '{}' install failed", addon.name())));
        }
    }

    // Phase 4: AGENTS.md (dynamically rendered, including addon segments)
    let skills_meta = collect_skill_frontmatters()?;
    let addon_refs: Vec<&dyn addons::Addon> = addon_list.iter().map(|b| b.as_ref()).collect();
    let agents_md_body = agents_md::render_agents_md(project_rules_body, &skills_meta, &addon_refs);
    let agents_target = dir.join("AGENTS.md");
    if let Err(e) = session.write(&agents_target, agents_md_body.as_bytes(), backup_existing) {
        session.rollback();
        return Err(e);
    }

    // Phase 5: install log
    let log_target = dir.join(".trae/.superpowers-install.log");
    let log_body = render_install_log(session.commit_actions_view());
    let _ = std::fs::create_dir_all(log_target.parent().unwrap());
    let _ = std::fs::write(&log_target, log_body);

    print_success(dir, skills_meta.len());
    Ok(())
}
```

> 关键变化：Phase 3 是新的 addon install；Phase 4 的 `render_agents_md` 调用现在传 `&addon_refs`。

- [ ] **Step 2: 跑测试**

```bash
cd cli && cargo build 2>&1 | tail -5
cd cli && cargo test 2>&1 | grep "test result"
```

Expected:
- 编译通过
- 全部 PASS（19 v0.1 + 10 v0.2 单测 + 13 v0.1 集成）

- [ ] **Step 3: 手工验证 init --addons ddd**

```bash
cd cli && cargo build
TMP=$(mktemp -d)
./target/debug/superpowers-trae init --dir "$TMP" --addons ddd
ls "$TMP/.trae/skills/ddd/"
ls "$TMP/" | grep -E "DOMAIN|README-DDD"
grep "DDD methodology" "$TMP/AGENTS.md" && echo "AGENTS.md has DDD section"
rm -rf "$TMP"
```

Expected:
- `.trae/skills/ddd/` 含 ddd-storm / ddd-model / ddd-spec
- DOMAIN.md + README-DDD-HARNESS.md 在项目根
- AGENTS.md 含 "DDD methodology" 字符串

- [ ] **Step 4: 验证 v0.1 路径不破——init 不带 --addons**

```bash
TMP=$(mktemp -d)
./target/debug/superpowers-trae init --dir "$TMP"
ls "$TMP/.trae/skills/" | grep -v ddd  # 只有 superpowers/
[ ! -f "$TMP/DOMAIN.md" ] && echo "no DOMAIN.md (expected)"
grep "DDD methodology" "$TMP/AGENTS.md" || echo "AGENTS.md has no DDD section (expected)"
rm -rf "$TMP"
```

Expected: 输出三行确认（"only superpowers" / "no DOMAIN.md" / "no DDD section"）。

- [ ] **Step 5: Commit**

```bash
git add cli/src/commands/init.rs
git -c user.name="xiaojin" -c user.email="amwtke@gmail.com" commit -m "feat(init): Phase 3 addon install + AGENTS.md 接收 addons"
```

---

## Task 7: status::run 加 Addons 段

**Files:**
- Modify: `cli/src/commands/status.rs`

- [ ] **Step 1: 改 run 函数**

在 `cli/src/commands/status.rs` 的 `pub fn run(dir_arg: &str) -> Result<()>` 内，把版本号头部更新 + 在 `println!("Addons: none");` 之前替换 Addons 检测逻辑。

把整个 `run` 函数替换为：

```rust
pub fn run(dir_arg: &str) -> Result<()> {
    let dir = PathBuf::from(dir_arg).canonicalize()?;

    let version = env!("CARGO_PKG_VERSION");
    let upstream_version = "5.0.7";
    println!(
        "superpowers-trae v{} (embedded superpowers {}, ddd plugin v{})",
        version, upstream_version, version
    );
    println!("Project: {}", dir.display());
    println!("─────────────────────────────────────────────────────────");

    let mut all_ok = true;

    let rules = dir.join(".trae/rules/project_rules.md");
    if rules.exists() {
        let size = fs::metadata(&rules).map(|m| m.len()).unwrap_or(0);
        println!("{} project_rules.md         (.trae/rules/, {} bytes)", "✓".green().bold(), size);
    } else {
        println!("{} project_rules.md         MISSING", "✗".red().bold());
        all_ok = false;
    }

    let skills_root = dir.join(".trae/skills/superpowers");
    let skill_count = count_skill_dirs(&skills_root);
    if skill_count == EXPECTED_SKILL_COUNT {
        println!("{} superpowers skills       {} / {} expected", "✓".green().bold(), skill_count, EXPECTED_SKILL_COUNT);
    } else {
        println!("{} superpowers skills       {} / {} expected", "✗".red().bold(), skill_count, EXPECTED_SKILL_COUNT);
        all_ok = false;
    }

    let agents_md = dir.join("AGENTS.md");
    if agents_md.exists() {
        let size = fs::metadata(&agents_md).map(|m| m.len()).unwrap_or(0);
        println!("{} AGENTS.md                (project root, {} bytes)", "✓".green().bold(), size);
    } else {
        println!("{} AGENTS.md                MISSING", "✗".red().bold());
        all_ok = false;
    }

    let log = dir.join(".trae/.superpowers-install.log");
    if log.exists() {
        if let Ok(meta) = fs::metadata(&log) {
            if let Ok(modified) = meta.modified() {
                let secs = modified.duration_since(std::time::UNIX_EPOCH).unwrap_or_default().as_secs();
                println!("{} Last init/upgrade        epoch {} (from install log mtime)", "ℹ".blue().bold(), secs);
            }
        }
    }

    println!();

    // Addons section
    check_addons(&dir, &mut all_ok);

    if !all_ok {
        anyhow::bail!("status: one or more required files missing");
    }
    Ok(())
}

fn check_addons(dir: &Path, all_ok: &mut bool) {
    let ddd_skills_root = dir.join(".trae/skills/ddd");
    let domain_md = dir.join("DOMAIN.md");
    let readme = dir.join("README-DDD-HARNESS.md");

    let has_any_ddd = ddd_skills_root.is_dir() || domain_md.exists() || readme.exists();
    if !has_any_ddd {
        println!("Addons: none");
        return;
    }

    println!("Addons:");

    let ddd_skill_count = count_skill_dirs(&ddd_skills_root);
    let ddd_complete = ddd_skill_count == 3 && domain_md.exists() && readme.exists();

    if ddd_complete {
        println!("  {} ddd", "✓".green().bold());
    } else {
        println!("  {} ddd (incomplete)", "⚠".yellow().bold());
        *all_ok = false;
    }

    println!("    - skills:    {} / 3", ddd_skill_count);

    if domain_md.exists() {
        println!("    - DOMAIN.md  (project root, user-managed)");
    } else {
        println!("    - DOMAIN.md  MISSING (run `superpowers-trae upgrade --addons ddd` to restore template)");
    }

    if readme.exists() {
        println!("    - README-DDD-HARNESS.md (project root)");
    } else {
        println!("    - README-DDD-HARNESS.md MISSING");
    }
}
```

> 注：`check_addons` 需要 `Path` import——文件顶部已经 `use std::path::{Path, PathBuf};`，OK。

- [ ] **Step 2: 跑测试**

```bash
cd cli && cargo build && cargo test 2>&1 | grep "test result"
```

Expected: 全部 PASS。

- [ ] **Step 3: 手工验证**

```bash
cd cli && cargo build
TMP=$(mktemp -d)
./target/debug/superpowers-trae init --dir "$TMP" --addons ddd
./target/debug/superpowers-trae status --dir "$TMP"
echo "---"
./target/debug/superpowers-trae status --dir "$(mktemp -d)" 2>&1 | head -10  # uninitialized → should fail
rm -rf "$TMP"
```

Expected:
- 第一个 status 输出含 "Addons:" 段 + ddd ✓ + 3/3 + DOMAIN.md + README
- 第二个 status 失败（base 检查就不通过）

- [ ] **Step 4: Commit**

```bash
git add cli/src/commands/status.rs
git -c user.name="xiaojin" -c user.email="amwtke@gmail.com" commit -m "feat(status): Addons 段检测 ddd 安装状态"
```

---

## Task 8: 集成测试 integration_addons_ddd.rs

**Files:**
- Create: `cli/tests/integration_addons_ddd.rs`

- [ ] **Step 1: 写文件**

```rust
//! End-to-end tests for `--addons ddd`.

use assert_cmd::Command;
use predicates::prelude::*;
use std::fs;
use tempfile::tempdir;

fn cli() -> Command {
    Command::cargo_bin("superpowers-trae").unwrap()
}

#[test]
fn init_addons_ddd_writes_ddd_skills() {
    let tmp = tempdir().unwrap();
    cli()
        .args(["init", "--dir", tmp.path().to_str().unwrap(), "--addons", "ddd"])
        .assert()
        .success();

    assert!(tmp.path().join(".trae/skills/ddd/ddd-storm/SKILL.md").exists());
    assert!(tmp.path().join(".trae/skills/ddd/ddd-model/SKILL.md").exists());
    assert!(tmp.path().join(".trae/skills/ddd/ddd-spec/SKILL.md").exists());
}

#[test]
fn init_addons_ddd_writes_domain_md() {
    let tmp = tempdir().unwrap();
    cli()
        .args(["init", "--dir", tmp.path().to_str().unwrap(), "--addons", "ddd"])
        .assert()
        .success();

    let body = fs::read_to_string(tmp.path().join("DOMAIN.md")).unwrap();
    assert!(body.contains("领域模型"));  // template should contain Chinese DDD vocabulary
    assert!(body.contains("Single Source of Truth"));
}

#[test]
fn init_addons_ddd_writes_readme_harness() {
    let tmp = tempdir().unwrap();
    cli()
        .args(["init", "--dir", tmp.path().to_str().unwrap(), "--addons", "ddd"])
        .assert()
        .success();

    assert!(tmp.path().join("README-DDD-HARNESS.md").exists());
}

#[test]
fn init_addons_ddd_extends_agents_md() {
    let tmp = tempdir().unwrap();
    cli()
        .args(["init", "--dir", tmp.path().to_str().unwrap(), "--addons", "ddd"])
        .assert()
        .success();

    let body = fs::read_to_string(tmp.path().join("AGENTS.md")).unwrap();
    assert!(body.contains("AGENTS DIRECTIVE"));
    assert!(body.contains("DDD methodology"));
    assert!(body.contains("Ubiquitous Language"));
}

#[test]
fn upgrade_addons_ddd_preserves_user_domain_md() {
    let tmp = tempdir().unwrap();
    cli()
        .args(["init", "--dir", tmp.path().to_str().unwrap(), "--addons", "ddd"])
        .assert()
        .success();

    // User edits DOMAIN.md
    let domain = tmp.path().join("DOMAIN.md");
    fs::write(&domain, "USER MODEL CONTENT — should be preserved").unwrap();

    cli()
        .args(["upgrade", "--dir", tmp.path().to_str().unwrap(), "--addons", "ddd"])
        .assert()
        .success();

    let body = fs::read_to_string(&domain).unwrap();
    assert_eq!(body, "USER MODEL CONTENT — should be preserved");
}

#[test]
fn upgrade_without_addons_keeps_existing_ddd_files() {
    let tmp = tempdir().unwrap();
    cli()
        .args(["init", "--dir", tmp.path().to_str().unwrap(), "--addons", "ddd"])
        .assert()
        .success();

    // User modifies a ddd skill (simulating user changes; even though static files,
    // user might do this)
    let storm = tmp.path().join(".trae/skills/ddd/ddd-storm/SKILL.md");
    fs::write(&storm, "USER MODIFIED DDD SKILL").unwrap();

    // upgrade without --addons should not touch ddd files
    cli()
        .args(["upgrade", "--dir", tmp.path().to_str().unwrap()])
        .assert()
        .success();

    let body = fs::read_to_string(&storm).unwrap();
    assert_eq!(body, "USER MODIFIED DDD SKILL");
}
```

- [ ] **Step 2: 跑测试**

```bash
cd cli && cargo test --test integration_addons_ddd 2>&1 | tail -10
```

Expected: 6 个全 PASS。

- [ ] **Step 3: 跑全套**

```bash
cd cli && cargo test 2>&1 | grep "test result"
```

Expected:
- unit 测试 PASS（v0.1 19 + v0.2 单测 + 集成测试 13 + 6 = 全 PASS）

- [ ] **Step 4: Commit**

```bash
git add cli/tests/integration_addons_ddd.rs
git -c user.name="xiaojin" -c user.email="amwtke@gmail.com" commit -m "test(addons): 6 个 DDD 端到端集成测试"
```

---

## Task 9: 更新 integration_status.rs 加 ddd 用例

**Files:**
- Modify: `cli/tests/integration_status.rs`

- [ ] **Step 1: 追加 1 个测试到现有文件末尾**

在 `cli/tests/integration_status.rs` 末尾追加：

```rust
#[test]
fn status_with_ddd_reports_addon() {
    let tmp = tempdir().unwrap();
    cli()
        .args(["init", "--dir", tmp.path().to_str().unwrap(), "--addons", "ddd"])
        .assert()
        .success();

    cli()
        .args(["status", "--dir", tmp.path().to_str().unwrap()])
        .assert()
        .success()
        .stdout(predicate::str::contains("Addons:"))
        .stdout(predicate::str::contains("ddd"))
        .stdout(predicate::str::contains("3 / 3"));
}
```

- [ ] **Step 2: 跑测试**

```bash
cd cli && cargo test --test integration_status 2>&1 | tail -5
```

Expected: 4 PASS（3 v0.1 原有 + 1 新增）。

- [ ] **Step 3: Commit**

```bash
git add cli/tests/integration_status.rs
git -c user.name="xiaojin" -c user.email="amwtke@gmail.com" commit -m "test(status): 加 ddd 安装时的 status 用例"
```

---

## Task 10: Makefile e2e-smoke 加 --addons ddd

**Files:**
- Modify: `Makefile`

- [ ] **Step 1: 找到 e2e-smoke 目标**

打开 `Makefile`，找到：

```makefile
e2e-smoke: cli-build
	@TMP=$$(mktemp -d) && \
	$(CLI_BIN) init --dir "$$TMP" && \
	$(CLI_BIN) status --dir "$$TMP" && \
	$(CLI_BIN) upgrade --dir "$$TMP" && \
	$(CLI_BIN) status --dir "$$TMP" && \
	rm -rf "$$TMP" && \
	echo "✓ e2e smoke passed"
```

替换为：

```makefile
e2e-smoke: cli-build
	@TMP=$$(mktemp -d) && \
	$(CLI_BIN) init --dir "$$TMP" --addons ddd && \
	$(CLI_BIN) status --dir "$$TMP" && \
	$(CLI_BIN) upgrade --dir "$$TMP" --addons ddd && \
	$(CLI_BIN) status --dir "$$TMP" && \
	test -f "$$TMP/DOMAIN.md" && \
	test -f "$$TMP/.trae/skills/ddd/ddd-storm/SKILL.md" && \
	test -f "$$TMP/README-DDD-HARNESS.md" && \
	rm -rf "$$TMP" && \
	echo "✓ e2e smoke passed (with ddd)"
```

- [ ] **Step 2: 跑 e2e-smoke**

```bash
cd /home/xiaojin/workshop/cc-superpower-to-trae   # repo 根（不在 cli/ 里）
make e2e-smoke
```

Expected: 末尾输出 `✓ e2e smoke passed (with ddd)`。

- [ ] **Step 3: Commit**

```bash
git add Makefile
git -c user.name="xiaojin" -c user.email="amwtke@gmail.com" commit -m "build: e2e-smoke 覆盖 --addons ddd 路径"
```

---

## Task 11: Cargo.toml version bump + ddd_run_version metadata

**Files:**
- Modify: `cli/Cargo.toml`

- [ ] **Step 1: 取 ddd-run 当前 commit short hash**

```bash
( cd ~/workshop/ddd-run && git rev-parse --short HEAD )
```

输出例：`a1b2c3d`（实际值由当前 ddd-run 仓库决定）。把它记下来——下面 Step 2 用。

- [ ] **Step 2: 改 cli/Cargo.toml**

打开 `cli/Cargo.toml`，找：

```toml
[package]
name = "superpowers-trae"
version = "0.1.0"
```

改为：

```toml
[package]
name = "superpowers-trae"
version = "0.2.0"
```

找：

```toml
[package.metadata.upstream]
superpowers_version = "5.0.7"
```

改为（追加一行）：

```toml
[package.metadata.upstream]
superpowers_version = "5.0.7"
ddd_run_version = "<step1 输出的 hash>"
```

把 `<step1 输出的 hash>` 替换为你 Step 1 取到的实际 commit hash，比如 `"a1b2c3d"`。

- [ ] **Step 3: 验证 version**

```bash
cd cli && cargo build
./target/debug/superpowers-trae --version
```

Expected: `superpowers-trae 0.2.0`。

- [ ] **Step 4: 验证 status 输出含 ddd plugin 版本**

```bash
TMP=$(mktemp -d)
./target/debug/superpowers-trae init --dir "$TMP" --addons ddd
./target/debug/superpowers-trae status --dir "$TMP" 2>&1 | head -3
rm -rf "$TMP"
```

Expected: 第一行 `superpowers-trae v0.2.0 (embedded superpowers 5.0.7, ddd plugin v0.2.0)`。

- [ ] **Step 5: 跑全套测试**

```bash
make test
make e2e-smoke
```

Expected: 全部 PASS。

- [ ] **Step 6: Commit**

```bash
git add cli/Cargo.toml cli/Cargo.lock
git -c user.name="xiaojin" -c user.email="amwtke@gmail.com" commit -m "chore(version): bump to v0.2.0 + 记录 ddd-run commit"
```

---

## Task 12: README 更新（仓库根 + cli/）

**Files:**
- Modify: `README.md`（仓库根）
- Modify: `cli/README.md`

- [ ] **Step 1: 改 README.md（仓库根）—— 在"## 快速开始（end-user）"末尾追加 DDD plugin 段**

打开 `README.md`，找到 "## Maintainer 工作流" 之前的位置（即 "## 3. 在 Trae IDE 里调用" 段末尾），追加：

```markdown
### 4. （可选）启用 DDD plugin

如果你要用 DDD（领域驱动设计）方法论先建模、再交给 superpowers 实现：

```bash
# 第一次 init 同时装 DDD
superpowers-trae init --addons ddd

# 已 init 后追加
superpowers-trae upgrade --addons ddd
```

写入额外文件：
- `.trae/skills/ddd/{ddd-storm,ddd-model,ddd-spec}/SKILL.md` — 3 个 DDD skill
- `DOMAIN.md` — 领域模型 SSOT（**只在不存在时安装**，后续 upgrade 不会覆盖你的模型）
- `README-DDD-HARNESS.md` — DDD 工作流向导
- `AGENTS.md` 末尾追加 "DDD methodology" 元规则段

工作流：
```
业务需求 → 使用 ddd-storm <描述>（事件风暴）
       → 使用 ddd-model（建领域模型，更新 DOMAIN.md）
       → 使用 ddd-spec <场景>（生成 spec）
       → 使用 superpowers brainstorming（决定技术栈）
       → superpowers writing-plans / executing-plans（TDD 实施）
```

DDD plugin 移植自 [ddd-run](https://github.com/amwtke/ddd-run)。
```

> 注意 4-backtick 嵌套问题——上面外层 ```markdown 用 3 反引号，里面的 ```bash 与 ``` 也是 3 反引号。这是合法 markdown（GitHub render 正常）；如果出现 fence 提前关闭，把外层改 4 反引号。

- [ ] **Step 2: 改 cli/README.md**

打开 `cli/README.md`，在 "## Invocation in Trae" 段之前追加：

```markdown
## DDD plugin (v0.2+)

Add the DDD methodology harness via `--addons ddd`:

```bash
superpowers-trae init --addons ddd        # first-time init with DDD
superpowers-trae upgrade --addons ddd     # add DDD to an already-initialized project
superpowers-trae status                   # shows "Addons: ddd ✓" when installed
```

`DOMAIN.md` is install-once: once you start filling it with your domain model, subsequent `upgrade --addons ddd` runs will preserve your content (template never overwrites).

```

- [ ] **Step 3: 验证两个 README**

```bash
grep "DDD plugin" README.md cli/README.md
```

Expected: 两个文件都有 "DDD plugin" 字符串。

- [ ] **Step 4: Commit**

```bash
git add README.md cli/README.md
git -c user.name="xiaojin" -c user.email="amwtke@gmail.com" commit -m "docs(readme): 加 DDD plugin 使用段（root + cli/README）"
```

---

## Self-Review

完成所有 12 个 Task 后：

- [ ] 跑全套测试

```bash
make test
make e2e-smoke
```

Expected: 全部 PASS。

- [ ] 把 spec 中的目标逐一勾对：
  - [x] §2 目标 (1) `init --addons ddd` 写入 5 项 ✔（Task 1, 4, 6）
  - [x] §2 目标 (2) `upgrade --addons ddd` 备份+刷 + DOMAIN.md 跳过 ✔（Task 4, 6, 8）
  - [x] §2 目标 (3) `upgrade` 不带 flag 不动 DDD ✔（Task 8 测试）
  - [x] §2 目标 (4) `status` Addons 段 ✔（Task 7, 9）
  - [x] §2 目标 (5) v0.1 行为兼容 ✔（agents_md_empty_addons_unchanged 测试）
  - [x] §2 目标 (6) Addon trait ✔（Task 3, 4）
  - [x] §2 目标 (7) version 0.2.0 + metadata ✔（Task 11）
  - [x] §2 目标 (8) 集成测试覆盖 ✔（Task 8 6 个 + Task 9 1 个）

- [ ] 真机：在 test-superpowers 项目跑 `superpowers-trae upgrade --addons ddd`，提示词里说"使用 ddd-storm 帮我梳理 X 业务"，预期 Trae Builder 主动 Read `.trae/skills/ddd/ddd-storm/SKILL.md` 并按 ddd-storm SKILL 流程走（手工 smoke）。

---

## 实施前注意

按 subagent-driven-development skill 要求：**先建 worktree** 再开始任务执行。Branch 名建议 `feat/ddd-plugin-v0-2-0`。

```bash
cd /home/xiaojin/workshop/cc-superpower-to-trae
git worktree add .worktrees/feat-ddd-plugin-v0-2-0 -b feat/ddd-plugin-v0-2-0
cd .worktrees/feat-ddd-plugin-v0-2-0
```
